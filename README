epresent.el --- Simple presentation mode for Emacs Org-mode

  1) load-file epresent.el
  2) open present.org
  3) call `epresent-run'
  4) press "t" to view the top level of the presentation
  5) navigate the presentation with "c", "n", "p", "t", "b" and 
     quit with "q"
